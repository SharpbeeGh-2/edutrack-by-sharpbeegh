export function SuperAdminDashboardPage() {
  return (
    <div>
      <h1 className="text-h3 font-semibold">Platform Dashboard</h1>
      <p className="mt-2 text-body-md text-gray-400">
        Real metrics (active schools, MRR, churn) land in Phase 1.
      </p>
    </div>
  )
}
